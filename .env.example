# ============================================================
# SYNCBOARD ENVIRONMENT VARIABLES TEMPLATE
# Copy to .env for local development
# ============================================================

# Environment
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# CORS origin(s) allowed to call the API (comma-separated for multiple)
CLIENT_URL=http://localhost:3001

# Database (PostgreSQL)
DATABASE_URL="postgresql://syncuser:syncpass@localhost:5432/syncboard?schema=public"
DATABASE_REPLICA_URL="postgresql://syncuser:syncpass@localhost:5433/syncboard?schema=public"

# Authentication & Keys
# RS256 key files are REQUIRED in production. In development, if the key files
# do not exist, the app falls back to HS256 using JWT_SECRET.
JWT_PRIVATE_KEY_PATH="keys/private.pem"
JWT_PUBLIC_KEY_PATH="keys/public.pem"
JWT_SECRET="dev-only-secret-replace-me"

# Redis Cache & Token Blacklist
REDIS_HOST=localhost
REDIS_PORT=6379

# RabbitMQ Event Bus (Phase 5)
RABBITMQ_URL="amqp://guest:guest@localhost:5672"

# Google OAuth (Optional for local testing)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
GOOGLE_CALLBACK_URL="http://localhost:3000/api/auth/google/callback"
